# Bench Scaffolding

`bench/sql/bench_distance.sql` provides a starting point for measuring DTW query costs under controlled synthetic workloads.

This is intentionally scaffolding in v1. It is not a full benchmark suite and does not claim external-library parity validation.
